# process the data into the correct format (identical to output of get_backtesting_data)

import pandas as pd 

def prepare_data(live_data):

    print('test')
    # training_df = pd.read_json?.iloc[::-1]
    # training_df.index = pd.to_datetime(training_df["Time"])

    # return training_df