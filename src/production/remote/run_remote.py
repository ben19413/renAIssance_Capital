# retrieve price data via:
# if deploy_remote:
#     api call to trading bot 
# else:
#     read local csv 

# make features
# call train classifier (input: data) (output: buy, sell, no trade)
# call train ATR (input: data) (output: stop loss)

# Return prediction, stop loss, take profit via:
# if deploy_remote:
#     responding to API call 
# else:
#     make new local csv 

