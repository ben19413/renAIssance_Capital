# time = now.datetime.datetime()

# while True: 
    # if new hour:
        # retrieve price data via:
        # process_local_data (input: file path) (output: data in same format as data after filtering in backtesting)

        # make features
        # call train classifier (input: data) (output: buy, sell, no trade)
        # call train ATR (input: data) (output: stop loss)

        # Return timestamp, prediction, stop loss, take profit via making new local csv

        # time = now.datetime.datetime()

    # sleep 1 minute

