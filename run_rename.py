# Get live price data from either:
#      local csv file OR api call to trading bot

# make features
# call train classifier (input: data) (output: buy, sell, no trade)
# call train ATR (input: data) (output: stop loss)


# Return prediction, stop loss, take profit via either:
#          putting csv in place OR responding to API call 
